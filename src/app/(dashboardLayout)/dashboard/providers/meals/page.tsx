import { DashboardPageShell } from "@/components/shared/dashboard-page-shell";

export default function ProviderMealsPage() {
  return (
    <DashboardPageShell
      title="Provider Meals"
      description="See all meals owned by the provider account and manage publishing, availability, and updates here."
    />
  );
}
