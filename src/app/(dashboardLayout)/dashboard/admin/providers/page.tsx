import { DashboardPageShell } from "@/components/shared/dashboard-page-shell";

export default function AdminProvidersPage() {
  return (
    <DashboardPageShell
      title="Providers"
      description="Review provider accounts, inspect provider activity, and manage provider-related administration tasks."
    />
  );
}
